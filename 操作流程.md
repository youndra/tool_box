# 操作流程

## 一、目标

把图片中的流程图、结构图、模块图，转换成可编辑的 PowerPoint 文件。

这里的“可编辑”指的是：

- 方框可以改
- 文字可以改
- 连线可以改
- 区块颜色可以改
- 整体布局可以继续拖动调整

而不是把原图直接贴进 PPT 里。

## 二、输出规范

每张图片都对应一个同名文件夹。

文件夹内建议包含：

- 原图
- 同名 `.json`
- 同名 `.pptx`

例如：

```text
outputs/
└─ CA/
   ├─ CA.png
   ├─ CA.json
   └─ CA.pptx
```

## 三、环境准备

```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

## 四、推荐工作流

### 第 1 步：准备图片

把图片放到项目中，或者放到你容易引用的路径。

例如：

```text
CA/CA.png
EMA注意力/EMA注意力.jpg
yolov8模块细节图/yolov8模块细节图.png
```

### 第 2 步：自动创建骨架

如果你只有图片，还没有 JSON，可以先运行：

```powershell
python .\scripts\bootstrap_from_image.py .\CA\CA.png --output-root .\outputs
```

这会自动创建：

```text
outputs/
└─ CA/
   ├─ CA.png
   └─ CA.json
```

### 第 3 步：编辑 JSON

JSON 用于描述 PPT 中的可编辑对象。

常见字段如下：

- `theme`
  用于定义页面大小、字体、颜色等基础样式

- `overlays`
  用于放置背景块、分区块、虚线框等装饰元素

- `segments`
  用于画自由直线

- `nodes`
  用于放置主要流程框、模块框、功能块

- `edges`
  用于连接节点

- `texts`
  用于补充独立文本说明

### 第 4 步：生成 PPTX

JSON 准备好之后，运行：

```powershell
python .\scripts\generate_pptx.py `
  --image .\CA\CA.png `
  --spec .\CA\CA.json `
  --output-root .\outputs `
  --copy-image
```

执行后会生成：

```text
outputs/
└─ CA/
   ├─ CA.png
   ├─ CA.json
   └─ CA.pptx
```

## 五、检查结果

打开生成后的 PPTX，重点检查以下内容：

- 每个框是否能单独选中
- 每段文字是否能直接修改
- 连线位置是否合理
- 标注文字是否有遮挡
- 布局是否接近原图逻辑

## 六、简单图与复杂图的建议

### 简单图

建议流程：

1. 先写最小 JSON
2. 先生成 PPT
3. 再在 PPT 中手工微调

### 复杂图

建议流程：

1. 先把图片拆成几个大模块
2. 先还原主结构
3. 再补文本和辅助线
4. 最后统一做视觉调整

## 七、适合上传到 GitHub 的内容

建议保留这些文件：

- `scripts/`
- `README.md`
- `操作流程.md`
- `requirements.txt`
- `LICENSE`
- 示例图片
- 示例 JSON
- 示例 PPTX

## 八、当前项目定位

这个项目当前是一套“半自动图片转可编辑 PPT”的工具链。

重点在于：

- 输出结果可编辑
- JSON 结构可复用
- 适合持续迭代
- 适合作为以后自动化识图工具的基础
