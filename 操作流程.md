# 操作流程

## 目标

把图片中的流程图、结构图或模块图，转换成可编辑的 PowerPoint 文件。

输出规则：

- 每张图片对应一个同名文件夹
- 文件夹内至少包含：
  - 同名 `.json`
  - 同名 `.pptx`
- 如果需要，也可以把原图一起复制进去

例如：

```text
outputs/
└─ CA/
   ├─ CA.png
   ├─ CA.json
   └─ CA.pptx
```

## 环境准备

```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

## 生成步骤

### 1. 准备原图

把原图放到项目根目录，或者任意你方便引用的位置。

例如：

```text
CA.png
EMA注意力.jpg
yolov8模块细节图.png
```

### 2. 自动创建骨架

运行：

```powershell
python .\scripts\bootstrap_from_image.py .\CA\CA.png
```

这会自动创建：

```text
outputs/
└─ CA/
   ├─ CA.png
   └─ CA.json
```

### 3. 编写或调整 JSON 规格

JSON 用于描述 PPT 中每个元素的位置、尺寸、文本和颜色。

核心字段包括：

- `theme`
- `overlays`
- `segments`
- `nodes`
- `edges`
- `texts`

### 4. 生成 PPTX

运行：

```powershell
python .\scripts\generate_pptx.py `
  --image .\CA.png `
  --spec .\CA\CA.json `
  --output-root .\outputs `
  --copy-image
```

### 5. 检查输出

查看：

- `outputs\CA\CA.json`
- `outputs\CA\CA.pptx`

打开 PowerPoint 后，确认这些内容都可以单独编辑：

- 方框
- 文本
- 连线
- 标注
- 背景区块

## 推荐操作方式

如果图比较简单：

1. 先手写一个最小 JSON
2. 生成 PPT
3. 在 PPT 里微调

如果图比较复杂：

1. 先拆成几个大模块
2. 先还原主结构
3. 再补文字和连线
4. 最后微调视觉细节

## 仓库分享建议

建议上传这些内容：

- `scripts/`
- `requirements.txt`
- `README.md`
- `操作流程.md`
- 示例图片
- 示例 JSON
- 示例 PPTX

## 当前定位

这是一个“半自动重建可编辑 PPT”的项目。

重点不是完全自动识图，而是：

- 结果可编辑
- 结构可复用
- JSON 可持续修改
- 适合持续迭代成更自动化的工具
