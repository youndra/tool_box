# 操作流程

## 一、先判断你属于哪类用户

这个项目有两种用法：

1. 你有 Codex 这类 agent
2. 你没有 agent，只能使用网页 AI

两种方式最后都会走到同一步：

`JSON -> PPTX`

区别只在前半段：

- 有 agent：`图片 -> agent -> JSON`
- 没有 agent：`图片 -> 网页 AI -> JSON`

## 二、公共准备

### 1. 安装依赖

```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

### 2. 准备图片

例如：

```text
CA/CA.png
```

### 3. 可选：先创建输出骨架

```powershell
python .\scripts\bootstrap_from_image.py .\CA\CA.png --output-root .\outputs
```

执行后会生成：

```text
outputs/
└─ CA/
   ├─ CA.png
   └─ CA.json
```

## 三、有 agent 的用户怎么做

### 第 1 步：让 agent 看图

把图片给 agent，并让它参考：

- `JSON_SCHEMA.md`
- 示例 JSON
- `PROMPT_TEMPLATE.md` 中的格式要求

### 第 2 步：让 agent 生成 JSON

推荐把 JSON 保存成：

```text
CA/CA.json
```

或者：

```text
outputs/CA/CA.json
```

### 第 3 步：生成 PPTX

如果 JSON 在 `CA/CA.json`，运行：

```powershell
python .\scripts\generate_pptx.py `
  --image .\CA\CA.png `
  --spec .\CA\CA.json `
  --output-root .\outputs `
  --copy-image
```

如果 JSON 在 `outputs/CA/CA.json`，运行：

```powershell
python .\scripts\generate_pptx.py `
  --image .\CA\CA.png `
  --spec .\outputs\CA\CA.json `
  --output-root .\outputs `
  --copy-image
```

## 四、没有 agent、只有网页 AI 的用户怎么做

### 第 1 步：打开网页 AI

打开任意支持图片输入的网页 AI。

### 第 2 步：上传图片

上传你的目标图片，例如：

```text
CA/CA.png
```

### 第 3 步：复制提示词

把 `PROMPT_TEMPLATE.md` 中的提示词完整复制给网页 AI。

目标是让网页 AI：

- 只输出 JSON
- 不输出解释
- 不输出 Markdown 代码块

### 第 4 步：保存网页 AI 输出的 JSON

网页 AI 输出 JSON 后，手动保存为同名文件。

例如：

```text
CA/CA.json
```

或者保存为：

```text
outputs/CA/CA.json
```

这一步非常关键。

网页 AI 生成 JSON 后，不能停在网页上，必须把它真正保存成 `.json` 文件，后面的 PPT 生成脚本才能使用。

### 第 5 步：生成 PPTX

如果 JSON 在 `CA/CA.json`，运行：

```powershell
python .\scripts\generate_pptx.py `
  --image .\CA\CA.png `
  --spec .\CA\CA.json `
  --output-root .\outputs `
  --copy-image
```

如果 JSON 在 `outputs/CA/CA.json`，运行：

```powershell
python .\scripts\generate_pptx.py `
  --image .\CA\CA.png `
  --spec .\outputs\CA\CA.json `
  --output-root .\outputs `
  --copy-image
```

### 第 6 步：查看结果

最终输出会在：

```text
outputs/CA/CA.pptx
```

同时通常也会保留：

```text
outputs/CA/CA.png
outputs/CA/CA.json
```

## 五、输出规则

统一规则如下：

- 每张图片对应一个同名文件夹
- 文件夹中保存同名 `.json`
- 文件夹中保存同名 `.pptx`
- 如有需要，也可以复制原图进去

例如：

```text
outputs/
└─ yolov8模块细节图/
   ├─ yolov8模块细节图.png
   ├─ yolov8模块细节图.json
   └─ yolov8模块细节图.pptx
```

## 六、检查结果

打开 PPTX 后，重点检查：

- 每个框是否能单独编辑
- 每段文字是否能直接修改
- 连线是否位置合理
- 标注是否遮挡
- 整体结构是否与原图逻辑一致
