# AI使用说明

## 这份项目与 AI 的关系

这个项目目前不是“完全自动图片识别工具”，而是：

`AI 辅助理解图片 -> 生成 JSON -> 脚本生成可编辑 PPTX`

也就是说：

- `JSON -> PPTX` 由脚本自动完成
- `图片 -> JSON` 目前主要依赖 AI 理解和辅助建模

## 为什么不同 AI 可能效果不同

因为这一步依赖模型本身的能力：

- 图像理解能力
- 空间布局理解能力
- 结构抽象能力
- 长 JSON 输出稳定性

所以换不同模型时，效果通常会有差异。

## 哪些模型更适合

更适合的模型通常具备这些特征：

- 支持多模态看图
- 能稳定输出结构化 JSON
- 对复杂图中的模块关系理解较好
- 不容易在长输出时丢字段

## 如果不是 GPT，还能不能用

可以。

但前提是对方使用的模型：

- 能看图
- 能理解结构关系
- 能按指定格式输出 JSON

如果模型只支持文本，不支持图片，那就无法直接完成“图片 -> JSON”。

## 如何提高不同模型之间的一致性

建议同时提供下面三样东西：

1. `JSON_SCHEMA.md`
2. `PROMPT_TEMPLATE.md`
3. 示例图片 + 示例 JSON

这样即使换模型，也更容易产出接近格式的结果。

## 推荐使用流程

### 方案 A：AI 直接出第一版 JSON

1. 给模型图片
2. 给模型 `JSON_SCHEMA.md`
3. 给模型 `PROMPT_TEMPLATE.md`
4. 让模型输出第一版 JSON
5. 人工检查并微调
6. 用脚本生成 PPTX

### 方案 B：先让 AI 拆结构，再人工补齐

适合更复杂的图：

1. 先让模型识别大区域
2. 再识别主要节点
3. 再补连线与文字
4. 最后人工整理成完整 JSON

## 实际建议

如果你想把这个项目分享给别人使用，建议在 README 中明确说明：

- 这是一个“AI 辅助 + JSON 中间层 + PPT 自动生成”的项目
- 并不是所有模型都能达到完全一样的效果
- 结果质量会受到模型能力影响

## 本项目当前的最佳定位

最合适的描述方式是：

这是一个用于“把图片图示重建为可编辑 PowerPoint”的半自动工具链。

优点是：

- 可编辑
- 可复用
- 可逐步自动化

不足是：

- 目前仍依赖 AI 辅助做图片结构理解

## 如果以后想继续增强

未来可以继续加入：

- `image_to_json.py` 初版自动识别脚本
- OCR 文本检测
- 视觉模型接口
- 坐标自动估计
- JSON 自动校验器
