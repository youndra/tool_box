# AI使用说明

## 项目当前定位

这个项目目前是一套“AI 辅助 + JSON 中间层 + PPT 自动生成”的工具链。

也就是说，它分成两段：

1. `图片 -> JSON`
2. `JSON -> PPTX`

其中：

- 第 2 段已经脚本化
- 第 1 段目前仍然依赖 AI 辅助理解图片结构

## 不是必须使用 Codex

当前并不是必须使用 Codex 才能完成整个流程。

更准确地说，只要有一种 AI 能满足下面几点，就可以参与第 1 步：

- 能看图片
- 能理解图中的结构关系
- 能按要求输出 JSON

所以项目支持两种使用方式。

## 方式一：有 agent

如果你使用的是 Codex 这类 agent：

- agent 可以直接看图
- agent 可以参考仓库里的 JSON 规范
- agent 可以直接输出 JSON
- agent 还可以继续调用脚本生成 PPTX

这是最顺滑的方式。

## 方式二：没有 agent，只有网页 AI

如果你没有类似 Codex 的 agent，也仍然可以完成。

推荐流程：

1. 打开一个支持看图的网页 AI
2. 上传图片
3. 把 `PROMPT_TEMPLATE.md` 里的提示词发给它
4. 让它只输出 JSON
5. 把结果保存成同名 `.json`
6. 回到本项目运行生成脚本

也就是说，没有 agent 时，网页 AI 可以充当“图片 -> JSON”的那一段。

## 推荐配套文件

为了让不同模型尽量输出一致，建议一起提供这几份内容：

- `JSON_SCHEMA.md`
- `PROMPT_TEMPLATE.md`
- 示例图片
- 示例 JSON

这几样东西的作用分别是：

- `JSON_SCHEMA.md`
  约束输出字段和格式

- `PROMPT_TEMPLATE.md`
  提供可直接复制到网页 AI 的提示词

- 示例图片 + 示例 JSON
  帮助模型模仿项目的风格和结构

## 不同模型效果为什么会不同

因为第 1 步目前仍然依赖模型能力。

影响效果的因素包括：

- 图像理解能力
- 空间布局理解能力
- 长 JSON 输出稳定性
- 是否容易漏字段

所以：

- 不是所有模型都能达到完全一样的效果
- 但并不要求必须是 GPT

## 最推荐的仓库说明方式

如果别人 clone 这个项目，最准确的理解应该是：

这是一个支持两种前端入口的项目：

1. `agent -> JSON -> PPTX`
2. `网页 AI -> JSON -> PPTX`

只要前端能产出符合规范的 JSON，后续脚本都能继续工作。

## 实际建议

如果你希望别人最容易复现：

1. 让他先看 `README.md`
2. 再看 `JSON_SCHEMA.md`
3. 没有 agent 时就看 `PROMPT_TEMPLATE.md`
4. 最后按 `操作流程.md` 生成 PPTX

## 当前边界

当前仓库还不是“纯脚本全自动识图工具”。

它的真实能力边界是：

- 已经实现：`JSON -> PPTX`
- 仍依赖 AI：`图片 -> JSON`

但通过 schema、提示词和示例文件，已经可以让不同 AI 比较稳定地接入这条流程。
