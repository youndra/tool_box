# PROMPT_TEMPLATE

这个文件提供给任何支持看图的 AI 模型使用。

目标是让模型尽量稳定地把图片转换成本项目可接受的 JSON。

## 通用提示词模板

把下面模板发给任意支持图片输入的模型，并附上目标图片：

```text
你要把这张图片中的流程图、结构图或模块图，转换成一个 JSON 对象。

要求：
1. 只输出 JSON，不要输出解释，不要加 Markdown 代码块。
2. JSON 必须符合我提供的字段结构：
   - title
   - theme
   - overlays
   - segments
   - nodes
   - edges
   - texts
3. 所有坐标单位统一使用英寸。
4. 保持图中主要结构、模块层级、连线方向和文字标签。
5. 不要求像素级复刻，但要求逻辑结构正确、布局尽量接近原图。
6. 所有 nodes 的 id 必须唯一。
7. 对于背景块、虚线框、灰色区域，放到 overlays。
8. 对于主要流程框、模块框、算子框，放到 nodes。
9. 对于独立的尺寸标注、说明文字、标题，放到 texts。
10. 对于无法直接挂靠到节点的自由直线，放到 segments。
11. 对于节点之间的连接关系，放到 edges。

补充要求：
- 如果图中包含表格，可以用多个 rect 节点模拟单元格。
- 如果图很复杂，优先保留主结构和主文字。
- 如果某些细小箭头或细节不确定，可以适度简化，但不能破坏主流程。
- theme 中默认使用白底，字体可优先使用 Microsoft YaHei 或 Arial。

请直接输出最终 JSON。
```

## 更稳定的增强版提示词

如果模型容易输出废话、漏字段或格式不稳定，可以改用下面版本：

```text
任务：读取图片中的结构图，并输出一个可用于生成 PowerPoint 的 JSON。

严格要求：
- 只输出一个合法 JSON 对象
- 不要输出任何解释
- 不要输出 ```json
- 不要省略顶层字段
- 即使某个数组为空，也要保留

顶层字段固定为：
{
  "title": "",
  "theme": {},
  "overlays": [],
  "segments": [],
  "nodes": [],
  "edges": [],
  "texts": []
}

字段约束：
- nodes 中每个对象必须有：id, shape, text, x, y, w, h
- edges 中每个对象必须有：from, to
- texts 中每个对象必须有：text, x, y
- overlays 用于背景块、虚线框、装饰区域
- segments 用于自由直线
- 所有颜色写成 #RRGGBB
- 所有坐标和尺寸写成数字

建模顺序：
1. 先识别大区块
2. 再识别主要节点
3. 再识别连接关系
4. 最后补充尺寸说明和文字标签

输出目标：
- 保证结构正确
- 保证 JSON 可直接继续编辑
- 保证后续脚本能把它生成成可编辑 PPT

现在直接输出 JSON。
```

## 使用建议

建议把下面几个文件一起给模型看：

- 图片本身
- `JSON_SCHEMA.md`
- 一个或多个示例 JSON

这样不同模型更容易学会你项目的输出格式。

## 推荐工作方式

如果模型能力较强：

1. 直接使用“通用提示词模板”
2. 输出第一版 JSON
3. 再人工微调 JSON

如果模型能力一般：

1. 先让它只识别大区域和主节点
2. 再让它补 `texts`
3. 再让它补 `edges`
4. 最后合并结果

## 适配不同模型时的现实情况

不同模型常见差异：

- 有的模型擅长读图，但容易漏坐标细节
- 有的模型格式化能力好，但空间关系较弱
- 有的模型会解释很多，需要更强的“只输出 JSON”约束

所以建议总是：

- 给 schema
- 给示例
- 给固定提示词
- 给清晰的输出限制
